<Window x:Class="SignalApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="신호 상태판"
        Height="300"
        Width="400">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <!-- 상태 표시 -->
        <StackPanel Grid.Row="0"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">

            <TextBlock Name="StatusText"
                       Text="정지"
                       FontSize="40"
                       FontWeight="Bold"
                       Foreground="Red"
                       HorizontalAlignment="Center"/>

            <Ellipse Name="SignalLamp"
                     Width="100"
                     Height="100"
                     Fill="Red"
                     Margin="0,20,0,0"/>
        </StackPanel>

        <!-- 버튼 영역 -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

            <Button Content="운행 시작"
                    Width="120"
                    Height="40"
                    Margin="10"
                    Click="StartButton_Click"/>

            <Button Content="정지"
                    Width="120"
                    Height="40"
                    Margin="10"
                    Click="StopButton_Click"/>

        </StackPanel>

    </Grid>

</Window>